# In production the frontend calls /api/** on its own origin.
# Firebase Hosting rewrites those requests to the Cloud Run API service,
# so no cross-origin call (and no CORS) is needed.
VITE_API_URL=
